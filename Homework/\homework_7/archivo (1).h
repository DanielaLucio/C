#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>
#define UNAME 8
#define Work 13
#define MAX 100
/* typedef struct{
     int pin;
     int entrada_salida;
     char *fecha;
     char *hora;
     int mes;
 }workers;
 workers *trab;*/
 
 void pin_registration();
 void attendance_list(int t);
 void timecome(int pin, char t);
 int difftimes(int pin);
 
 
